import { MainContainer } from "../components/container/main.container"
import { QueueIcon } from "../lib/icons/app.icons"

export const RegistryPage = (): JSX.Element => {

    return (
        <MainContainer title="Registro de Turno" icon={QueueIcon}>
            
        </MainContainer>
    )
}