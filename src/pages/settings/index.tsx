import { MainContainer } from "../../components/container/main.container"
import { SettingsIcon } from "../../lib/icons/main.icons"


export const SettingPage = (): JSX.Element => {

    return (
        <MainContainer title="Ajustes del Sistema" icon={SettingsIcon}>

        </MainContainer>
    )
}